{
  config,
  lib,
  pkgs,
  ...
}:

{
  # Hardware configuration for Intel iMac 27" 2013 (minimal for darwin)
  # Most hardware is managed by macOS directly
  # Running macOS Sequoia via OpenCore Legacy Patcher (OCLP)
}
